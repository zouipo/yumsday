services:
  yumsday:
    build:
      context: ..
      target: runtime
    pull_policy: build
    container_name: yumsday
    image: yumsday:latest
    user: 1000:1000
    ports:
      - 8080:8080
    volumes:
      - ./data:/data
