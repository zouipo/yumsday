services:
  yumsday:
    build:
      context: ..
      target: runtime
    pull_policy: build
    container_name: yumsday
    image: yumsday:latest
    ports:
      - 8080:8080
    volumes:
      - ./data:/data
